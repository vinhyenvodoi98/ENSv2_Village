import type { WorldState } from "../state/useWorldStore";

/**
 * Road network maintenance (e.g. rebuilding paths when a fortress moves).
 * Stub for task 20 — populated in task 25. Plain function over state, no JSX.
 */
export function tickRoads(state: WorldState): void {
  void state;
}
