import type { WorldState } from "../state/useWorldStore";

/**
 * Per-tick cart movement along roads. Stub for task 20 — populated when
 * roads/carts are introduced (task 25). Plain function over state, no JSX.
 */
export function tickCarts(state: WorldState): void {
  void state;
}
