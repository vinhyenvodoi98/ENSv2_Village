import type { WorldState } from "../state/useWorldStore";

/**
 * Per-tick weather transitions. Stub for task 20 — populated in task 28.
 * Plain function over state, no JSX.
 */
export function tickWeather(state: WorldState): void {
  void state;
}
