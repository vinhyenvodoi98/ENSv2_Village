import type { WorldState } from "../state/useWorldStore";

/**
 * Per-tick citizen movement/behavior. Stub for task 20 — populated when
 * citizens are introduced (task 26). Plain function over state, no JSX.
 */
export function tickCitizens(state: WorldState): void {
  void state;
}
