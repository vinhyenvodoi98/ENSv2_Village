"use client";

/**
 * Dev-only debug panel (leva, if installed). Stub for task 20 — built in
 * task 22, gated behind `process.env.NODE_ENV === 'development'`.
 */
export function DebugPanel() {
  return null;
}
