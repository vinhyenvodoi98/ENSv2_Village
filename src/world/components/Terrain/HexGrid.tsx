"use client";

/**
 * Instanced hex grid, driven by useWorldStore's tiles. Stub for task 20 —
 * populated in task 21 once `<Instances>` batching is wired up. Task 20 only
 * needs the flat `<Ground>` plane.
 */
export function HexGrid() {
  return null;
}
