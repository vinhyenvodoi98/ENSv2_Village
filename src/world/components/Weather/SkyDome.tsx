"use client";

/**
 * Sky dome (drei `<Sky>` wrapper, theme-driven). Task 20 uses a plain
 * `Canvas` background/fog color instead; this stub is built out in task 28.
 */
export function SkyDome() {
  return null;
}
